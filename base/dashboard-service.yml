apiVersion: v1
kind: Service
metadata:
  name: argo-rollouts-dashboard
  namespace: argo-rollouts
spec:
  selector:
    app: argo-rollouts-dashboard
  ports:
    - protocol: TCP
      port: 80        
      targetPort: 3100 
  type: ClusterIP 

